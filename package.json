{
	"name": "virus-simulator",
	"private": true,
	"scripts": {
		"predev": "bash scripts/free-dev-ports.sh",
		"dev": "concurrently -n backend,frontend -c blue,magenta \"npm run dev:backend\" \"npm run dev:frontend\"",
		"dev:backend": "cd backend && ./gradlew bootRun",
		"dev:frontend": "cd frontend && npm run dev"
	},
	"devDependencies": {
		"concurrently": "^9.2.0"
	}
}
